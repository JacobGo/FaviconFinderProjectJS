name: favicon-finder-project-js
region: nyc
services:
  - dockerfile_path: Dockerfile
    github:
      branch: master
      deploy_on_push: true
      repo: JacobGo/FaviconFinderProjectJS
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xs
    name: favicon-finder-project-js
    routes:
      - path: /
    source_dir: /
